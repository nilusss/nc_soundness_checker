import maya.cmds as mc
import maya.mel as mel
import nc_soundness_checker as ch

from nc_soundness_checker import soundness_checker as sc
from nc_soundness_checker import ncsc_files
from nc_soundness_checker.ncsc_files import ncsc_API as api
from nc_soundness_checker.ncsc_files import ncsc_geo
from nc_soundness_checker.ncsc_files import ncsc_transform
from nc_soundness_checker.ncsc_files import ncsc_naming
from nc_soundness_checker.ncsc_files import ncsc_uv

reload(ch)
reload(ncsc_files)
reload(api)
reload(ncsc_geo)
reload(ncsc_transform)
reload(ncsc_naming)
reload(ncsc_uv)

sc.openui()

ncsc_transform.ncsc_freeze_trans()

obj_space = mc.xform('pCylinder6_geo', q=True, ws=True, rp=True)
offset = [x * -1 for x in obj_space]
mc.xform('pCylinder6_geo', ws=True, t=offset)


sc.compile_ui(r"C:\Users\nilas\Documents\maya\2019\scripts\nc_soundness_checker\ncsc_ui", "soundness_checker_ui.ui")
